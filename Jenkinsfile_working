pipeline {
    agent any
	tools {
		maven 'Maven'
	}
	
	environment {
		dockerImage =''
		registry = "hellcasterexe/modelserver" 
	        registryCredential = 'dockerhub_id' 
			
	}

	environment {
		dockerImage =''
		registry = "hellcasterexe/webserver" 
	        registryCredential = 'dockerhub_id' 
			
	}
	
    stages {
	    stage('Scm Checkout') {
		    steps {
			    checkout scm
		    }
	    }
	    
	    stage('Build') {
		    steps {
			    sh 'mvn clean package'
		    }
	    }
	    
	    
	    
	    stage('Building our image') { 
            	steps { 
                	script { 
                    		dockerImage = docker.build registry + ":$BUILD_NUMBER" 
                	}
            	} 
           }
	    
	    
	   
    }
}
